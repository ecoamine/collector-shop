# Kubernetes manifests (skeleton)

This directory will contain Kubernetes manifests for deploying the Collector Shop application.

At this stage, only the structure is defined. Detailed manifests for deployments, services, ingress, and configuration will be added later.

